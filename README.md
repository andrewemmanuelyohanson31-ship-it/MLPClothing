# MLPClothing
The study adopts a supervised learning approach. The model learns a mapping between body measurements and clothing size labels. Training uses labeled datasets consisting of anthropometric inputs and corresponding garment sizes. The methodology follows an experimental design. Multiple models are trained and evaluated under the same conditions. 
